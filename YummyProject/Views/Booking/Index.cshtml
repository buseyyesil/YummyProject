@model List<YummyProject.Models.Booking>
@{
    ViewBag.Title = "Onay Bekleyen Rezervasyonlar";
    Layout = "~/Views/AdminLayout/Index.cshtml";
}

<h2>Onay Bekleyen Rezervasyonlar</h2>
<br />

@if (TempData["SuccessMessage"] != null)
{
    <div class="alert alert-success">@TempData["SuccessMessage"]</div>
}

<table class="table table-bordered table-hover">
    <thead>
        <tr>
            <th>#</th>
            <th>Ad Soyad</th>
            <th>Email</th>
            <th>Telefon</th>
            <th>Tarih</th>
            <th>Kişi Sayısı</th>
            <th>Mesaj</th>
            <th>İşlemler</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var item in Model)
        {
            <tr>
                <td>@item.BookingId</td>
                <td>@item.Name</td>
                <td>@item.Email</td>
                <td>@item.PhoneNumber</td>
                <td>@item.BookingDate.ToString("dd.MM.yyyy HH:mm")</td>
                <td>@item.PersonCount kişi</td>
                <td>@item.MessageContent</td>
                <td>
                    <a href="/Booking/ApproveBooking/@item.BookingId" class="btn btn-success btn-sm">Onayla</a>
                    <a href="/Booking/BookingDetails/@item.BookingId" class="btn btn-info btn-sm">Detay</a>
                    <a href="/Booking/DeleteBooking/@item.BookingId" class="btn btn-danger btn-sm" onclick="return confirm('Silmek istediğinize emin misiniz?')">Sil</a>
                </td>
            </tr>
        }
    </tbody>
</table>

@if (Model.Count == 0)
{
    <div class="alert alert-info">
        Onay bekleyen rezervasyon bulunmamaktadır.
    </div>
}